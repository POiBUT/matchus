# Amvera Cloud Configuration for Matchus Telegram Mini App

meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  artifacts:
    "": ""
run:
  command: npm run start
  persistenceMount: /data
  containerPort: 80
  servicePort: 80

