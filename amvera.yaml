# Amvera Cloud Configuration for Matchus Telegram Mini App

meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  artifacts:
    - path: /
      pattern: "*"

run:
  command: npm run start
  persistenceMount: /data
  containerPort: 80
  servicePort: 80
